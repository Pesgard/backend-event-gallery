# PostgreSQL (Aurora) connection string
DATABASE_URL="postgresql://user:password@host:5432/event_gallery?schema=public"

# JWT
JWT_SECRET="replace_with_strong_secret"
JWT_EXPIRES_IN="7d"

# Prisma
# (optional) set DEBUG="prisma:client"
